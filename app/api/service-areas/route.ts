import { NextRequest, NextResponse } from 'next/server';
import { INBOX_SESSION_COOKIE,isInboxSessionValid } from '../../lib/inbox-auth';
export const dynamic='force-dynamic';
function cfg(){const apiBase=process.env.CHATPRO_API_URL?.trim().replace(/\/$/,'');const inboxKey=process.env.CHATPRO_INBOX_KEY?.trim();if(!apiBase||!inboxKey)throw new Error('Faltan CHATPRO_API_URL o CHATPRO_INBOX_KEY.');return{apiBase,inboxKey};}
async function run(r:NextRequest,m:'GET'|'POST'){if(!(await isInboxSessionValid(r.cookies.get(INBOX_SESSION_COOKIE)?.value)))return NextResponse.json({ok:false,error:'Sesión requerida.'},{status:401});try{const company=r.nextUrl.searchParams.get('company')?.trim().toLowerCase()??'';if(!company)return NextResponse.json({ok:false,error:'Falta la empresa.'},{status:400});const{apiBase,inboxKey}=cfg();const u=new URL(`${apiBase}/service-areas`);u.searchParams.set('company',company);const res=await fetch(u,{method:m,headers:{'x-chatpro-inbox-key':inboxKey,...(m==='POST'?{'content-type':'application/json'}:{})},body:m==='POST'?JSON.stringify(await r.json()):undefined,cache:'no-store'});return new NextResponse(await res.text(),{status:res.status,headers:{'content-type':res.headers.get('content-type')??'application/json'}})}catch(e){return NextResponse.json({ok:false,error:e instanceof Error?e.message:'Error'},{status:500});}}
export async function GET(r:NextRequest){return run(r,'GET')} export async function POST(r:NextRequest){return run(r,'POST')}
